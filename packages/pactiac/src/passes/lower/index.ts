/** Phase 10 — tag router via manifest ir metadata → WorkspaceIr (L2). */
export { lowerBoundTree, type LowerBoundTreeInput, type LowerBoundTreeResult } from "./lower-bound-tree.js";
export const passName = "lower" as const;
