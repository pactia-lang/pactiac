/** Phase 6 — resolve @ / #[ symbols against EffectiveRegistry → BoundTree (L1). */
export { bindSyntaxTree, type BindSyntaxTreeResult } from "./bind-syntax-tree.js";
export const passName = "bind" as const;
